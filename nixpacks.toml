[phases.setup]
nixPkgs = ["python311", "postgresql"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install -r requirements.txt"
]

[start]
# Start command будет переопределён в UI Railway для каждого сервиса
cmd = "python -m bot_api.main"
