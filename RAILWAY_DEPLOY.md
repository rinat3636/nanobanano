# 🚂 Деплой на Railway - Пошаговая инструкция

## 📋 Что потребуется

1. ✅ Аккаунт на [Railway.app](https://railway.app)
2. ✅ Аккаунт на [GitHub](https://github.com)
3. ✅ Telegram Bot Token от [@BotFather](https://t.me/BotFather)
4. ✅ Gemini API Key от [Google AI Studio](https://aistudio.google.com/)
5. ✅ ЮКасса Shop ID и Secret Key от [yookassa.ru](https://yookassa.ru)

---

## 🚀 Шаг 1: Подготовка кода

### 1.1 Загрузите код на GitHub

```bash
# Инициализируйте git репозиторий
cd nano_banana_pro
git init

# Добавьте .gitignore
cat > .gitignore << EOF
.env
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
data/
*.log
.DS_Store
EOF

# Коммит и пуш
git add .
git commit -m "Initial commit: Nano Banana Pro V2"

# Создайте репозиторий на GitHub и пушните
git remote add origin https://github.com/your-username/nano-banana-pro.git
git branch -M main
git push -u origin main
```

---

## 🏗 Шаг 2: Создание проекта на Railway

### 2.1 Создайте новый проект

1. Перейдите на [Railway.app](https://railway.app)
2. Нажмите **"New Project"**
3. Выберите **"Deploy from GitHub repo"**
4. Выберите ваш репозиторий `nano-banana-pro`

### 2.2 Добавьте PostgreSQL

1. В проекте нажмите **"New"** → **"Database"** → **"Add PostgreSQL"**
2. Railway автоматически создаст переменную `DATABASE_URL`

### 2.3 Добавьте Redis

1. В проекте нажмите **"New"** → **"Database"** → **"Add Redis"**
2. Railway автоматически создаст переменную `REDIS_URL`

---

## ⚙️ Шаг 3: Настройка переменных окружения

### 3.1 Получите Telegram Bot Token

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен (формат: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 3.2 Получите Gemini API Key

1. Перейдите на [Google AI Studio](https://aistudio.google.com/)
2. Нажмите **"Get API Key"**
3. Создайте новый ключ
4. Скопируйте API Key

### 3.3 Получите ЮКасса ключи

1. Зарегистрируйтесь на [yookassa.ru](https://yookassa.ru)
2. Создайте магазин
3. Перейдите в **Настройки** → **Ключи API**
4. Скопируйте:
   - `shopId` (ID магазина)
   - `secretKey` (секретный ключ)

### 3.4 Добавьте переменные в Railway

В настройках вашего сервиса на Railway добавьте:

```env
# Telegram
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_WEBHOOK_URL=https://your-app.railway.app/webhook/telegram
TELEGRAM_WEBHOOK_SECRET=any_random_string

# Gemini
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_TIMEOUT=600

# ЮКасса
YOOKASSA_SHOP_ID=123456
YOOKASSA_SECRET_KEY=live_xxxxxxxxxxxxxxxx
YOOKASSA_WEBHOOK_SECRET=your_webhook_secret_here
YOOKASSA_WEBHOOK_URL=https://your-app.railway.app/webhook/yookassa

# API
API_HOST=0.0.0.0

# Логирование
LOG_LEVEL=INFO
```

**⚠️ Важно:**
- Замените `your-app.railway.app` на реальный домен вашего приложения
- `TELEGRAM_WEBHOOK_SECRET` и `YOOKASSA_WEBHOOK_SECRET` - любые случайные строки

---

## 🔧 Шаг 4: Настройка сервисов

### 4.1 Создайте два сервиса

Railway позволяет запустить несколько процессов из одного репозитория.

#### Сервис 1: Bot API

1. В проекте нажмите **"New"** → **"GitHub Repo"** → Выберите репозиторий
2. Назовите сервис: `bot-api`
3. В **Settings** → **Deploy** → **Start Command**:
   ```
   python -m bot_api.main
   ```
4. Добавьте все переменные окружения из Шага 3.4

#### Сервис 2: Worker

1. В проекте нажмите **"New"** → **"GitHub Repo"** → Выберите репозиторий
2. Назовите сервис: `worker`
3. В **Settings** → **Deploy** → **Start Command**:
   ```
   python -m worker.main
   ```
4. Добавьте те же переменные окружения

### 4.2 Получите публичный URL

1. Откройте сервис `bot-api`
2. В **Settings** → **Networking** → **Generate Domain**
3. Скопируйте домен (например: `nano-banana-pro-production.up.railway.app`)
4. Обновите переменные:
   ```env
   TELEGRAM_WEBHOOK_URL=https://nano-banana-pro-production.up.railway.app/webhook/telegram
   YOOKASSA_WEBHOOK_URL=https://nano-banana-pro-production.up.railway.app/webhook/yookassa
   ```

---

## 💳 Шаг 5: Настройка ЮКасса Webhook

### 5.1 Настройте webhook в ЮКасса

1. Войдите в [личный кабинет ЮКасса](https://yookassa.ru/my)
2. Перейдите в **Настройки** → **HTTP-уведомления**
3. Добавьте URL:
   ```
   https://nano-banana-pro-production.up.railway.app/webhook/yookassa
   ```
4. Включите уведомления:
   - ✅ `payment.succeeded` (Платеж успешно завершен)
   - ✅ `payment.canceled` (Платеж отменен)
5. Нажмите **Сохранить**

### 5.2 Сгенерируйте webhook secret

```bash
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

Добавьте результат в переменную `YOOKASSA_WEBHOOK_SECRET` в Railway.

---

## 🧪 Шаг 6: Тестирование

### 6.1 Проверьте деплой

1. Откройте логи сервиса `bot-api` в Railway
2. Убедитесь что нет ошибок
3. Проверьте health endpoint:
   ```bash
   curl https://your-app.railway.app/health
   ```

### 6.2 Проверьте webhook

```bash
curl https://your-app.railway.app/webhook/telegram
```

Должен вернуть ошибку (это нормально, webhook принимает только POST от Telegram).

### 6.3 Проверьте бота

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Бот должен ответить приветственным сообщением

### 6.4 Проверьте платежи

1. Отправьте `/topup`
2. Выберите пакет
3. Нажмите **"Оплатить"**
4. Используйте [тестовые карты ЮКасса](https://yookassa.ru/developers/payment-acceptance/testing-and-going-live/testing)
5. Проверьте что кредиты начислились: `/balance`

---

## 📊 Шаг 7: Мониторинг

### 7.1 Логи

Railway автоматически собирает логи:

1. Откройте сервис в Railway
2. Перейдите в **Deployments** → **View Logs**

### 7.2 Метрики

Railway показывает:
- CPU usage
- Memory usage
- Network traffic

### 7.3 Алерты

Настройте уведомления в Railway:
1. **Settings** → **Notifications**
2. Включите уведомления о:
   - Deployment failures
   - Service crashes
   - High resource usage

---

## 🔄 Шаг 8: Обновление

### 8.1 Автоматический деплой

Railway автоматически деплоит при пуше в `main`:

```bash
git add .
git commit -m "Update bot"
git push origin main
```

### 8.2 Ручной деплой

1. В Railway откройте сервис
2. Нажмите **"Deploy"** → **"Redeploy"**

---

## 💰 Шаг 9: Тарифы Railway

### Free Plan
- ✅ $5 в месяц бесплатно
- ✅ До 500 часов работы
- ✅ Подходит для тестирования

### Hobby Plan ($5/месяц)
- ✅ $5 кредитов + $5 оплата
- ✅ Неограниченное время работы
- ✅ Подходит для небольших проектов

### Pro Plan ($20/месяц)
- ✅ $20 кредитов включено
- ✅ Priority support
- ✅ Подходит для production

**Рекомендация:** Начните с Free Plan, затем переходите на Hobby.

---

## 🐛 Troubleshooting

### Проблема: Бот не отвечает

**Решение:**
1. Проверьте логи в Railway
2. Убедитесь что `TELEGRAM_WEBHOOK_URL` правильный
3. Проверьте что сервис запущен
4. Удалите старый webhook:
   ```bash
   curl -X POST "https://api.telegram.org/bot<TOKEN>/deleteWebhook"
   ```

### Проблема: Платежи не обрабатываются

**Решение:**
1. Проверьте webhook в ЮКасса
2. Проверьте `YOOKASSA_WEBHOOK_URL`
3. Проверьте логи платежей
4. Убедитесь что webhook URL доступен:
   ```bash
   curl https://your-app.railway.app/webhook/yookassa
   ```

### Проблема: Worker не запускается

**Решение:**
1. Проверьте логи worker сервиса
2. Убедитесь что `REDIS_URL` правильный
3. Проверьте что Redis сервис запущен
4. Перезапустите worker сервис

### Проблема: Ошибка подключения к БД

**Решение:**
1. Проверьте что PostgreSQL сервис запущен
2. Убедитесь что `DATABASE_URL` установлен
3. Проверьте логи PostgreSQL
4. Перезапустите сервис

---

## 📈 Оптимизация для Production

### 1. Добавьте мониторинг

Интегрируйте [Sentry](https://sentry.io):

```python
import sentry_sdk
sentry_sdk.init(dsn="your-dsn")
```

### 2. Настройте автоскейлинг

Railway поддерживает горизонтальное масштабирование:
1. **Settings** → **Scaling**
2. Увеличьте количество реплик worker

### 3. Добавьте CDN для изображений

Используйте S3 + CloudFront:
1. Настройте S3 bucket
2. Обновите `shared/config.py`
3. Загружайте изображения в S3

### 4. Настройте rate limiting

Добавьте в `bot_api/main.py`:

```python
from slowapi import Limiter
limiter = Limiter(key_func=get_remote_address)
```

---

## ✅ Чеклист финального запуска

- [ ] Все переменные окружения установлены
- [ ] PostgreSQL и Redis запущены
- [ ] Bot API сервис запущен
- [ ] Worker сервис запущен
- [ ] Webhook Telegram настроен
- [ ] Webhook ЮКасса настроен
- [ ] Бот отвечает на `/start`
- [ ] Тестовый платеж прошел успешно
- [ ] Тестовая генерация работает
- [ ] Логи не содержат критических ошибок
- [ ] Мониторинг настроен

---

## 🎉 Готово!

Ваш бот теперь работает в production на Railway!

**Полезные ссылки:**
- [Railway Docs](https://docs.railway.app/)
- [ЮКасса API](https://yookassa.ru/developers/api)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Gemini API](https://ai.google.dev/docs)

**Поддержка:** @Bashirov1111
