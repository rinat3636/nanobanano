# Этот Procfile НЕ используется Railway!
# Railway использует Start Command из UI настроек каждого сервиса.
# Оставлен для локальной разработки с foreman/honcho.

web: python -m bot_api.main
worker: python -m worker.main
